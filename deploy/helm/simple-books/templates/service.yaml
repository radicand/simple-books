apiVersion: v1
kind: Service
metadata:
  name: {{ include "simple-books.fullname" . }}
  labels:
    {{- include "simple-books.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "simple-books.selectorLabels" . | nindent 4 }}
